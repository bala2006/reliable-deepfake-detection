# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Datasets - ROOT-ANCHORED so rlroinet/data/*.py stays tracked
/data/
/uploads/

# Model weights and packaged bundles (160 MB+ / 943 MB)
*.pt
*.pth
*.onnx
*.zip

# Cached features (regenerated, large)
**/feature_cache/
*.npz

# Keep: metrics JSON, diagnostics JSON, run_config.json, source, docs, tests
